# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ✅ 1. ADD THIS STEP: Set up the correct Node.js version
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Use the Node.js version your project requires

      # ✅ 2. ADD THIS STEP: Install your project's dependencies
      - name: Install Dependencies
        run: npm ci # 'ci' is faster and more reliable for workflows

      # This step will now succeed because Vite is installed
      - name: Build Project
        run: npm run build

      # This is your existing deployment step
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY_SYSTEMS_CLIENT_98765 }}
          channelId: live
          projectId: key-systems-client-98765